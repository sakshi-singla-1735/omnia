name: Ansible Lint

on:
  pull_request:
    paths:
      - '**/*.yml'
      - '**/*.yaml'

jobs:
  ansible-lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        fetch-depth: 0  # Fetch all history so the base commit is available

    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Get the SHA of the latest commit
      run: |
        commit_sha=$(git rev-parse HEAD)
        echo "commit_sha=$commit_sha"
      register: commit_sha_result
      changed_when: false

    - name: Set the commit SHA as a fact
      set_fact:
        commit_sha: "{{ commit_sha_result.stdout_lines[0].split('=')[1] }}"

    - name: Get list of changed YAML files
      id: changed-files
      run: |
        files=$(git diff --name-only ${{ github.event.pull_request.base.sha }}  {{ commit_sha }} | grep -E '\.ya?ml$' || echo '')
        echo "files=$files" >> $GITHUB_ENV

    - name: Run ansible-lint
      if: env.files != ''
      uses: ansible/ansible-lint-action@v6
      with:
        targets: ${{ env.files }}
        override-deps: |
          ansible==7.7.0
          ansible-lint==6.19.0
