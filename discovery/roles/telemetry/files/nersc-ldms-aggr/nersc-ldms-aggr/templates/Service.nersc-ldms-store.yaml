apiVersion: v1
kind: Service
metadata:
  name: nersc-ldms-store
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: nersc-ldms-store
  clusterIP: None
  ports:
    - protocol: TCP
      port: {{ .Values.store_port }}
      targetPort: {{ .Values.store_port }}

