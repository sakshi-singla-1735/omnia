apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: ome
  namespace: {{ telemetry_namespace }}
  labels:
    strimzi.io/cluster: "kafka"
spec:
  partitions: {{ kafka_topic_partitions['ome'] }}
  replicas: 2
  config:
    cleanup.policy: delete
